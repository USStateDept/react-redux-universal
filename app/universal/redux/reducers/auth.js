// import { 
//   REQUEST_DYNAMIC, REQUEST_DYNAMIC_SUCCESS, 
// } from '../actions/dynamic';

const initialState = {
  
};

export default function auth(state = initialState, action) {
  switch (action.type) {
  case "action_here variable":
    break;
  default:
    return state;
  }
}